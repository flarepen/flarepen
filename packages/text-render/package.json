{
    "name": "@flarepen/text-render",
    "version": "0.1.0",
    "private": true,
    "dependencies": {},
    "scripts": {
        "build": "wasm-pack build --target web || (. /root/.cargo/env && wasm-pack build --target web)"
    }
}