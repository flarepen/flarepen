{
    "name": "@flarepen/text-render",
    "version": "0.1.0",
    "private": true,
    "dependencies": {},
    "scripts": {
        "build": "wasm-pack build --target web || (export PATH=/root/.cargo/bin:$PATH && wasm-pack build --target web)"
    }
}